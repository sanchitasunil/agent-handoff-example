livekit-agents>=1.0
livekit-plugins-deepgram>=1.0
livekit-plugins-openai>=1.0
livekit-plugins-murf>=1.0
livekit-plugins-silero>=1.0
python-dotenv>=1.0

# livekit-agents 1.6.9 and livekit-plugins-openai import httpx but don't declare
# it; they relied on the openai package pulling it in. openai 3.x moved to
# httpx2, so on a fresh install `import livekit.agents` fails with
# ModuleNotFoundError: No module named 'httpx' unless it's requested directly.
httpx
